---
name: Bug report
about: Something in the CLI, SDK, daemon, or mock server misbehaves
labels: bug
---

## Description

<!-- What happened vs what you expected. -->

## Reproduction

```bash
# Exact command(s). Add HTTP_TRACE=1 output if relevant — REDACT keys, tokens, and fingerprints.
```

## Environment

- grid-cli version (`grid --version`):
- Install method (source / binary / npm):
- OS / Node version:

## Additional context

<!-- Logs (redacted), screenshots, config shape (no secrets). -->
